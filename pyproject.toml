[project]
name = "whatsapp-chatbot"
version = "0.1.0"
description = "Backend minimo para recibir webhooks de WhatsApp Cloud API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.1.0",
    "fastapi>=0.116.0",
    "httpx>=0.28.0",
    "uvicorn>=0.35.0",
]

[dependency-groups]
dev = []
